<ul>
	<li>
		{{#if session.isAuthenticated}}
		  <a {{ action 'invalidateSession' }}>Logout</a>
			<li>{{#link-to 'lunch' 7}}Lunch{{/link-to}}</li>
		{{else}}
		  {{#link-to 'login'}}Login{{/link-to}}
		{{/if}}
	</li>
</ul>

{{outlet}}
